services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "${MCC_PORT:-8880}:80"
    depends_on:
      - backend
    restart: unless-stopped
